POSTGRES_USER=admin
POSTGRES_PASSWORD=admin1234
POSTGRES_DB=test_db_app1
JWT_SECRET=your-secret-here
PORT=5000

# Public origin for absolute profile photo URLs (IsUrl in settings). Example: https://api.example.com
PUBLIC_APP_URL=http://localhost:5000

DATABASE_URL=postgresql://admin:admin1234@localhost:5432/test_db_app1?schema=public

# Swagger UI at /api/docs — production: omit or false (default off). Non-production: default on; set false to disable.
# SWAGGER_ENABLED=true
